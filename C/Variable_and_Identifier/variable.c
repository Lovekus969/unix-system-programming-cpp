#include<stdio.h>   // Includes the standard input-output library

int main() {
    int age;        // Step 1: Declare a variable 'age' of type integer
    age = 22;       // Step 2: Assign value 22 to 'age'
    
    // Step 3: Print the variable using printf and %d (format specifier for int)
    printf("Bhavneet is %d years old", age);
    
    return 0;       // Step 4: Return 0 to indicate successful execution
}
