#include <stdio.h>

int main() {

    // Initialize a double variable
    double PI = 3.1415;

   // Use %lf to print a double
   printf("The value of pi is %lf", PI); //use %lf (stands for long float)

   return 0;
}
